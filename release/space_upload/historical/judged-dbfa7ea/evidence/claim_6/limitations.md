# Claim 6 limitations and deviations

- This falsifies uniform generalization over the formal `MP_D` class described
  in Section 6.2, not the standard uniform law for a genuinely fixed Hölder
  class with a common envelope.
- Adding a uniform range/value-at-zero bound repairs this counterexample.
- The data distribution, loss, labels, dimensions, `L`, `D`, and `r` are fixed;
  only the model offset allowed by `MP_D` varies.
- No asymptotic inference is based on simulation; all probabilities are exact.

